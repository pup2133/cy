<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.cy.LoginMapper">

	<select id="login" resultType="Login">
		SELECT m_id ,m_pw
		FROM cy_member
    	where m_id = #{id}
	</select>
	
		<select id="findId" resultType="Login">
		SELECT m_name , m_email
		FROM cy_member
    	where m_name = #{name}
    	where m_email = #{email}
	</select>
      
    <select id="FindPw" resultType="Login" >
    	SELECT m_id , m_email
		FROM cy_member
    	where m_id = #{id}
    	where m_email = #{email}
    
    </select>
        
        
        
</mapper>