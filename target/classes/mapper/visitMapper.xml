<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.visitMapper">
    
<!--     <select id="count" resultType="int">
        SELECT count(*) FROM test112
    </select>
    
    <select id="selectAll" resultType="Member">
        SELECT *
        FROM test112        
    </select>
     
     <insert id="insert" parameterType="Member">
      INSERT INTO test112
          (id, pwd, name)
      VALUES
          (#{id}, #{pwd}, #{name})
    </insert>
  
    
     <update id="update" parameterType="Member">
        UPDATE test112
        SET   name = #{name} ,
              email= #{email)     
        WHERE id = #{id}
    </update>
    
     <delete id="deleteAll">
        DELETE FROM test112
    </delete>


    <delete id="delete" parameterType="String">
        DELETE FROM test112  WHERE id = #{id} 
    </delete> -->
    
    <select id="selectOne" resultType="visit">
    	select m_nick, h_pic
		from cy_member m
		join cy_home h
		on m.m_id = h.m_id
		where m.m_id = #{id}
    </select>
    
    <select id="selectVisit" resultType="visit">
    	select m.m_nick, h.h_pic, v.v_num, v.v_text, to_char(v.v_time,'yyyy-mm-dd hh24:mi:ss'), v.host_id, v.guest_id 
		from cy_visit v
		join cy_member m
		on v.guest_id = m.m_id
		join cy_home h
		on h.m_id = m.m_id
    </select>
    
    <select id="findMemberId" resultType="String">
        select m_id from cy_member where m_id = #{id}
    </select>
    
    <insert id="visitInsert" parameterType="visit">
    	insert into cy_visit
    		(v_num,v_text,v_time,host_id,guest_id) 
    	values
    		(SEQ_cy_visit_v_num.currval,#{v_text},systimestamp,#{v_hostId},#{v_guestId})
    </insert>
    
</mapper>